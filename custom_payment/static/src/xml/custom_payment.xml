<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="reconciliation.line.mv_line_edited">
        <tr t-if="line.display !== false"
            t-attf-class="mv_line #{line.is_liquidity_line ? ' already_reconciled' : ''} #{line.__invalid ? 'invalid' : ''} #{line.is_tax ? 'is_tax' : ''}"
            t-att-data-line-id="line.id" t-att-data-selected="selected">
            <td class="cell_account_code"><t t-esc="line.account_code"/>&#8203;
            </td> <!-- zero width space to make empty lines the height of the text -->
            <td class="cell_due_date">
                <t t-if="typeof(line.id) != 'number' &amp;&amp; line.id">
                    <span class="badge badge-secondary">New</span>
                </t>
                <t t-else="" t-esc="line.date_maturity || line.date"/>
            </td>
            <td class="cell_label">
                <t t-if="line.partner_id &amp;&amp; line.partner_id !== state.st_line.partner_id">
                    <t t-if="line.partner_name.length">
                        <span class="font-weight-bold"
                              t-esc="line.partner_name"/>:
                    </t>
                </t>
                <t t-esc="line.name"/>
                <t t-if="line.ref &amp;&amp; line.ref.length">:</t>
                <t t-esc="line.ref"/>
            </td>
            <td class="cell_left">
                <t t-if="line.amount &lt; 0">
                    <span t-att-class="((line.is_move_line || line.is_liquidity_line) &amp;&amp; proposition == true) ? 'cell' : ''">
                        <span class="line_amount">
                            <span t-if="line.amount_currency_str"
                                  t-attf-class="o_multi_currency o_multi_currency_color_#{line.currency_id%8} line_info_button fa fa-money"
                                  t-att-data-content="line.amount_currency_str"/>
                            <span t-if="line.partial_amount &amp;&amp; line.partial_amount != line.amount"
                                  class="strike_amount text-muted">
                                <t t-out="line.amount_str"/>
                                <br/>
                            </span>
                        </span>
                        <t t-if="(line.is_move_line || line.is_liquidity_line) &amp;&amp; proposition == true">
                            <i class="fa fa-pencil edit_amount"></i>
                            <input class="edit_amount_input text-right d-none"/>
                        </t>
                        <div style="position:relative;">
                            <span class="currency-code"
                                  style="position:absolute; left:8px;">
                                <t t-esc="line.amount_str[0]"
                                   for="lt_amount"/>
                            </span>
                            <input t-att-value="line.credit"
                                   type="text" t-att-id="line.name"
                                   style="padding-left:20px; border:solid 0px #ccc;"
                                   class="re_input o_field_char o_field_widget o_quick_editable o_input"/>
                        </div>
                    </span>
                </t>
            </td>
            <td class="cell_right">
                <t t-if="line.amount &gt; 0">
                    <t t-call="reconciliation.line.mv_line.amount"/>
                </t>
            </td>
            <td class="cell_info_popover"></td>
        </tr>
    </t>
    <t t-inherit="account_accountant.reconciliation.manual.line"
       t-inherit-mode="extension">
        <xpath expr="//div[hasclass('float-right')]" position="replace">
            <div class="float-right o_buttons">
                <button t-attf-class="o_validate btn btn-secondary #{!state.balance.type ? '' : 'd-none'}"
                        id="reconcile_btn1">
                    Reconcile
                </button>
                <button t-attf-class="o_reconcile btn btn-primary #{state.balance.type &gt; 0 ? '' : 'd-none'}"
                        id="reconcile_btn2">
                    Reconcile
                </button>
                <button t-attf-class="o_no_valid btn btn-secondary #{state.balance.type &lt; 0 ? '' : 'd-none'}">
                    Skip
                </button>
            </div>
        </xpath>
    </t>

<!--    <t t-name="reconciliation.line.mv_line.amount">-->
<!--        <span t-att-class="((line.is_move_line || line.is_liquidity_line) &amp;&amp; proposition == true) ? 'cell' : ''">-->
<!--            <span class="line_amount">-->
<!--                <span t-if="line.amount_currency_str"-->
<!--                      t-attf-class="o_multi_currency o_multi_currency_color_#{line.currency_id%8} line_info_button fa fa-money"-->
<!--                      t-att-data-content="line.amount_currency_str"/>-->
<!--                <span t-if="line.partial_amount &amp;&amp; line.partial_amount != line.amount"-->
<!--                      class="strike_amount text-muted">-->
<!--                    <t t-out="line.amount_str"/>-->
<!--                    <br/>-->
<!--                </span>-->
<!--            </span>-->
<!--            <t t-if="(line.is_move_line || line.is_liquidity_line) &amp;&amp; proposition == true">-->
<!--                <i class="fa fa-pencil edit_amount"></i>-->
<!--                <input class="edit_amount_input text-right d-none"/>-->
<!--            </t>-->
<!--            <t t-if="line.amount &lt; 0">-->
<!--                <p>changed</p>-->
<!--                <span class="line_amount">-->
<!--                    <t t-if="!line.partial_amount_str" t-out="line.amount_str"/>-->
<!--                    <t t-if="line.partial_amount_str &amp;&amp; line.partial_amount != line.amount"-->
<!--                       t-out="line.partial_amount_str"/>-->
<!--                </span>-->
<!--            </t>-->
<!--            <t t-if="line.amount &gt; 0">-->
<!--                <span class="line_amount">-->
<!--                    <t t-if="!line.partial_amount_str" t-out="line.amount_str"/>-->
<!--                    <t t-if="line.partial_amount_str &amp;&amp; line.partial_amount != line.amount"-->
<!--                       t-out="line.partial_amount_str"/>-->
<!--                </span>-->
<!--            </t>-->
<!--        </span>-->
<!--    </t>-->


</templates>
